name: Auto-add issue to Mocha project

# Automatically adds issues to our GitHub project.
# We silence "add" failures to reduce noise.
# (This often happens when issues are created from project GUI.)
# https://markwiemer.com/software/github/apps#auto-add

on:
  issues:
    types: [opened]

jobs:
  add-to-project:
    runs-on: ubuntu-latest
    steps:
      # https://github.com/actions/create-github-app-token
      - uses: actions/create-github-app-token@v3
        id: app-token
        with:
          # https://github.com/mochajs/mocha/settings/secrets/actions
          client-id: ${{ secrets.APP_CLIENT_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}
          owner: mochajs
      # https://github.com/actions/add-to-project
      - uses: actions/add-to-project@v2
        with:
          project-url: https://github.com/orgs/mochajs/projects/6
          github-token: ${{ steps.app-token.outputs.token }}
        continue-on-error: true
