name: Format

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
    types: [opened, synchronize, reopened, edited]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  format:
    uses: ./.github/workflows/npm-script.yml
    with:
      npm-script: format:check
